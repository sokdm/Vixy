{
  "name": "vixy-api",
  "version": "2.5.3",
  "private": true,
  "type": "module",
  "dependencies": {
    "@xmaxai/sdk-global": "^1.1.7",
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "build:app": "npm install --prefix app && npm run build:app --prefix app"
  }
}
